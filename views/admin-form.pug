extends layout 

block content
  div.admin-form 
    h1 Become an admin 
    p.pb Enter the secret password to gain admin status (if not already a member, admin status also grants member status)
    p.pb2 Only special people can become admins AKA no hints 
    form.input-form(action='' method='POST')
      div
        label(for='admin_password') Password 
        input#admin_password(type='password' name='admin_password' value=(admin_pass===undefined ? '' : admin_pass))
        if errors 
          for error in errors 
            if error.path == 'admin_password'
              span= error.msg
      button.form-bottom(type='submit') Submit 