extends layout 

block content 
  h1 Become an admin 
  p Enter the secret password to gain admin status (if not already a member, admin status also grants member status)
  p Only special people can become admins AKA no hints 
  form(action='' method='POST')
    div
      label(for='admin_password') Password 
      input#admin_password(type='password' name='admin_password' value=(admin_pass===undefined ? '' : admin_pass))
    button(type='submit') Submit 

  if errors 
    ul 
      for error in errors 
        li!= error.msg